WiFi-Astra — Wireless Security Assessment Framework
====================================================

/home/kali/Documents/Antigravity/WiFi_PT/
├── wifi-astra.sh              # Main Launcher & Orchestrator (aliased to wifi-astra)
├── install.sh                 # Global dependency installer & system config
├── Architecture.txt           # System design & file mapping
├── README.md                  # Project overview & usage guide
├── License.txt                # Build summary & legal notice
│
├── lib/                       # Core Framework Libraries
│   ├── config.sh              # TC Registry, global variables, and module sequencing
│   ├── logger.sh              # Unified logging engine (Console + Log files)
│   ├── session.sh             # Session persistence (JSON), target selection
│   ├── trap_handler.sh        # LIFO cleanup stack, signal handling
│   ├── network_stack.sh       # Interface & Connection Management
│   ├── report.sh              # JSON aggregation, TXT/HTML reports
│   ├── progress.sh            # Progress bars and countdowns
│   ├── ui_helpers.sh          # Adaptive scoping, manual annotations
│   ├── process_manager.sh     # Safe tool execution (run_attack_tool)
│   ├── evidence.sh            # Evidence hashing, registration, manifests
│   ├── menu.sh                # TUI system and interactive wizards
│   └── pcap.sh                # Packet capture validation
│
├── utils/                     # Helper Utilities
│   └── parsers/               # Python micro-parsers (Airodump, OUI, etc.)
│
├── tests/                     # Framework Validation
│   └── test_parsers.py        # Parser unit tests
│
├── evidence/                  # Default Base for Session Data
│   └── <session_id>/          # Single session container
│       ├── evidence/          # Artifacts (PCAPs, Logs, TXT) + .sha256 hashes
│       ├── logs/              # Execution logs (master.log, module.log)
│       ├── reports/           # Generated TXT/HTML reports
│       ├── results/           # Individual TC JSON results
│       ├── session.state      # Central session state (JSON)
│       └── evidence_manifest.json # SHA256 hashed artifact audit trail
└── wordlists/                 # Custom wordlists for auth testing
